<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Rubots</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <h1>Attrition</h1>
  
          <table class="table table-striped">
          <thead>
            <tr>
              <th>Player</th>
              <th>Last Move</th>
              <th>Soldiers Remaining</th>
              <th>Turns Survived</th>
            </tr>
          </thead>
          <tbody>
            <% settings.game.sorted.each do |player| %>
              <tr>
                <td><%= player.name %></td>
                <td><%= player.moves.last %></td>
                <td><%= player.score %></td>
                <td><%= player.moves.size %></td>
              </tr>
            <% end %>
          </tbody>
          </table>
          
          <p>
            <a class="btn btn-md btn-success" href="/play" role="button">Play</a>
            <a class="btn btn-md btn-danger" href="/reset" role="button">Reset</a>
          </p>
        </div>
      </div>
    </div>

    <script src="/js/jquery.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
  </body>
</html>
