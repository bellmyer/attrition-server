<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Rubots</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <h1>
            <a href="/">Attrition</a> - 
            <% if settings.game.round == 0 %>
              The Calm Before the Storm
            <% else %>
              Round <%= settings.game.round %>
            <% end %>
          </h1>
  
          <table class="table table-striped">
          <thead>
            <tr>
              <th>Rank</th>
              <th>Player</th>
              <th class="text-right">Last Move</th>
              <th class="text-right">Soldiers Remaining</th>
              <th class="text-right">Rounds Survived</th>
            </tr>
          </thead>
          <tbody>
            <% settings.game.sorted.each_with_index do |player, i| %>
              <tr>
                <td><%= i + 1 %>.</td>
                <td><%= player.name %></td>
                <td class="text-right"><%= player.moves.last %></td>
                <td class="text-right"><%= player.score %></td>
                <td class="text-right"><%= player.moves.size %></td>
              </tr>
            <% end %>
          </tbody>
          </table>
          
          <p>
            <% if settings.game.alive.size > 1 %>
              <a class="btn btn-md btn-success" href="/play" role="button">Play</a>
            <% end %>
            <a class="btn btn-md btn-danger" href="/reset" role="button">Reset</a>
          </p>
        </div>
      </div>
    </div>

    <script src="/js/jquery.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
  </body>
</html>
